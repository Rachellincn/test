运行手账

1.模型位置glxiang/.cache/modelscope/hub/models/Qwen/Qwen_3.5_0.8B

2.data位置glxiang/llm_peft/run/OpenMathReasoning

3测试集在run/math_benchmarks里面

拿十万条测试

```
    raw_dataset = load_dataset("parquet", data_files=TRAIN_DATA_FILES, split="train").select(range(10000))
    raw_dataset = load_dataset("parquet", data_files=TRAIN_DATA_FILES, split="train") 
```

四张卡CUDA_VISIBLE_DEVICES=1,2,3 NCCL_P2P_DISABLE=1 NCCL_IB_DISABLE=1 setsid torchrun --nproc_per_node=3 train_open_math.py > train.log 2>&1

```
NCCL_P2P_DISABLE=1 NCCL_IB_DISABLE=1 torchrun --nproc_per_node=4 train_open_math.py
```

三卡CUDA_VISIBLE_DEVICES=1,2,3 NCCL_P2P_DISABLE=1 NCCL_IB_DISABLE=1 torchrun --nproc_per_node=3 train_open_math.py

conda activate llm_peft
cd ~/llm_peft/run



# 1. 内存碎片优化

export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

# 2. 启动训练（注意我加了 --master_port=29501）

CUDA_VISIBLE_DEVICES=1,2,3 NCCL_P2P_DISABLE=1 NCCL_IB_DISABLE=1 torchrun --nproc_per_node=3 --master_port=29501 train_open_math.py > train.log 2>&1 &

# 3. 剥离终端控制

disown
